<h1>Recipes#new</h1>

<%= form_with model: @recipe do |f| %>
    <p>Name: <%= f.text_field :name %></p>
    <%= f.collection_select :user_id, @users, :id, :name %>
    <br>
    <%= f.submit %>
<% end %>
